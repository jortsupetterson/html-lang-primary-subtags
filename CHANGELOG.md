# Changelog

All notable changes to this project are documented here.

## 1.0.3 - 2025-12-17

- fix duplicate `PrimaryLanguageSubtag` export and keep the list compilable
- add missing primary language subtags (`ae`, `ak`, `sh`) while retaining legacy ones like `bh` and `jv`
- align package metadata for publish by including MIT license/changelog in the packed files
